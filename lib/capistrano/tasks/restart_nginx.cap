namespace :deploy do
  desc 'Restart nginx'
  task :restart_nginx => [:set_rails_env] do
    on roles(:app) do
      within release_path do
        with rails_env: fetch(:rails_env) do
          execute :sudo, '-E /etc/init.d/nginx restart'
        end
      end
    end
  end
end