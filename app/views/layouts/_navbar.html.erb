<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">LoadResult</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if params[:controller] == 'results' && action_name == "index"%>"><a href="/results">Results</a></li>
      </ul>
      <!-- only direct link
      <ul class="nav navbar-nav navbar-left">
        <li class="<%= 'active' if params[:controller] == 'project' && action_name == "index" %>">
          <%= link_to 'Projects', project_index_path %>
        </li>
      </ul>
      -->
      <ul class="nav navbar-nav navbar-left">
        <li class="<%= 'active' if (params[:controller] == 'performance_groups'||params[:controller] == 'performance_labels') && action_name == "index" %>">
          <%= link_to 'Performance groups and labels', performance_groups_path %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= 'active' if action_name == "new"%>">
          <%= link_to 'Upload results', new_result_path %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
