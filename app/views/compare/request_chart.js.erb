var current1 = {
    name: 'CURRENT: web00 EXEC ASP.NET / Requests',
    x: <%= @data[:current][:web00][:x].map(&:to_i) %>,
    y: <%= @data[:current][:web00][:y].map(&:to_i) %>,
    mode: 'lines',
    line: {
        color: 'red'
    }
};
var current2 = {
    name: 'CURRENT: web11 EXEC ASP.NET / Requests',
    x: <%= @data[:current][:web01][:x].map(&:to_i) %>,
    y: <%= @data[:current][:web01][:y].map(&:to_i) %>,
    mode: 'lines',
    line: {
        color: 'green'
    }
};
var base1 = {
    name: 'BASE: web00 EXEC ASP.NET / Requests',
    x: <%= @data[:base][:web00][:x].map(&:to_i) %>,
    y: <%= @data[:base][:web00][:y].map(&:to_i) %>,
    mode: 'lines',
    line: {
        color: 'red',
        dash: 'dot',
        width: 2
    }
};
var base2 = {
    name: 'BASE: web11 EXEC ASP.NET / Requests',
    x: <%= @data[:base][:web01][:x].map(&:to_i) %>,
    y: <%= @data[:base][:web01][:y].map(&:to_i) %>,
    mode: 'lines',
    line: {
        color: 'green',
        dash: 'dot',
        width: 2
    }
};
var layout = {
    title: '<%= @name %>',
    xaxis: {
        title: 'Time, s'
    },
    yaxis: {
        title: 'Metrics, %'
    }
};
Plotly.newPlot(<%= @div_id %>, [current1, current2, base1, base2], layout);
